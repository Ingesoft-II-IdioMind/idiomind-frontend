interface ModalProps {
    text: string;
    outlined?: boolean;
    type?: "button" | "submit" | "reset";
    haveIcon?: boolean;
  }
  
  export default function Modal () {
  
      return (
        <div>
            Modal
        </div>
      );
  }